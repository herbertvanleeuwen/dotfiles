# Export Spaceship theme SETTINGS
## SPACESHIP PYTHON SETTINGS
SPACESHIP_PYTHON_SHOW=false					                # Show section
SPACESHIP_PYTHON_ASYNC=true					                # Render section asynchronously
SPACESHIP_PYTHON_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX	# Section's prefix
SPACESHIP_PYTHON_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX	# Section's suffix
SPACESHIP_PYTHON_SYMBOL="🐍 "					            # Symbol displayed before the section
SPACESHIP_PYTHON_COLOR=yellow

## SPACESHIP VIRTUALENV SETTINGS
SPACESHIP_VENV_SHOW=true					                # Show section
SPACESHIP_VENV_ASYNC=false					                # Render section asynchronously
SPACESHIP_VENV_PREFIX=$SPACESHIP_PROMPT_DEFAULT_PREFIX		# Section's prefix
SPACESHIP_VENV_SUFFIX=$SPACESHIP_PROMPT_DEFAULT_SUFFIX		# Section's suffix
SPACESHIP_VENV_SYMBOL="🐍 "
SPACESHIP_VENV_GENERIC_NAMES=(virtualenv venv .venv) 		# Generic folder names. Parent folder will be used as name instead
SPACESHIP_VENV_COLOR=blue	

## SPACESHIP GCLOUD SETTINGS
SPACESHIP_GCLOUD_SHOW=true